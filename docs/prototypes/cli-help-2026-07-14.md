# PROTOTYPE v2 — dstow CLI help surface (revised after reaction session)

**Throwaway artifact** for [CLI surface: commands, flags, help text](https://github.com/rocne/dstow/issues/22).
v2 incorporates the full 2026-07-14 reaction session: all of D1–D14 resolved,
plus the session's structural decisions (shape rule, unified naming grammar,
`info`, the dependencies-group removal). This version is proposed as final
for the ticket resolution. Naming-grammar examples use `:` as the
repo/package separator **provisionally** — the separator itself belongs to
the naming-grammar ticket.

---

## The shape rule (structural, session-decided)

> Every command is a **group** or a **leaf**. Groups contain subcommands and
> take no operands (bare group prints its help). Leaves take operands and
> never have subcommands.

Consequence: operand/subcommand collision is impossible by construction.
Groups are nouns (`repo`, `snippet`); leaves are verbs plus the established
query names (`list`, `status`, `info` — glossary/lineage terms beat
grammatical purity).

## Naming (delegated to the naming-grammar ticket, assumed here)

- Fully qualified package name = source coordinate + package:
  `github:rocne/dotfiles:zsh` (separator provisional).
- **Any suffix that resolves uniquely is a valid name** — `zsh`,
  `dotfiles:zsh`, `rocne/dotfiles:zsh` — for packages and repos alike.
- A repo name and a package name are syntactically distinguishable (the
  separator guarantees it); paths announce themselves as paths
  (`/`, `~/`, `./` prefixes — ordinary Unix semantics).
- Naming a **repo** where packages are expected means *all its packages*.

---

## Top-level help

```
dstow — deploy dotfiles and configuration as symlinks, from packages in repos

Usage:
  dstow <command> [args] [flags]

Deploy:
  stow        Link packages into their targets
  unstow      Remove packages' links from their targets
  restow      Unstow, then stow again (refresh links)
  adopt       Import an existing file into a package, leaving a link behind

Inspect:
  list        What is configured: repos, packages, targets (never reads disk)
  info        Everything dstow knows about one repo or package
  status      What is deployed: live state of packages against their targets

Maintain:
  check       Verify every link in the ledger; classify broken and orphaned
  clean       Execute exactly what check reported (broken freely, orphans ask)
  rebuild     Reconstruct a lost ledger by walking configured targets (rare)

Groups:
  repo        Manage repos: add, remove, update, upgrade
  snippet     Print canned shell snippets: rc bootstrap, helper hooks

Also:
  completion  Generate shell completion (bash, zsh, fish, powershell)
  version     Print version

Global flags:
      --color <when>   Colorize output: auto (default), always, never
  -q, --quiet          Suppress informational output (announcements survive)
  -y, --yes            Assume "yes" at confirmation prompts
  -h, --help           Help for dstow or any command

Environment:
  DSTOW_PATH   Colon-separated repos prepended to the configured search path

Name packages and repos by any unambiguous suffix of their qualified name
(github:owner/repo:package). The working directory never changes what a
command does. See 'dstow <command> --help' for details and examples.
```

## Per-command help

### stow / unstow / restow (leaves)

```
Link packages into their targets.

Usage:
  dstow stow <name>... [flags]
  dstow stow --all

Names are packages or repos, by any unambiguous suffix of the qualified
name; naming a repo stows all of its packages. With no names, dstow asks
before stowing everything — in scripts that is an error: pass --all.

Each package succeeds or fails on its own; the run continues past failures
and exits nonzero if any package failed. Explicitly naming a package or
repo overrides its exclude-from-bulk setting.

Flags:
      --all       Every package in the resolved set, without asking
  -n, --dry-run   Show the plan; change nothing

Examples:
  dstow stow zsh git tmux
  dstow stow dotfiles              # a repo: all of its packages
  dstow stow work/dots:zsh         # qualified just enough to be unique
  dstow stow --all
```

(unstow and restow are identical in shape; bare `unstow` confirms with the
same care — it is the destructive one.)

### adopt (leaf)

```
Import an existing real file into a package; a link takes its place.
Live content always wins — adopt never destroys running configuration.

Usage:
  dstow adopt <file> [<package>] [flags]
  dstow adopt --occupied <package>

With a package: shows its plan and asks before overwriting differing
package content. Without a package: lists the packages that could adopt
the file, ranked, and asks you to pick (in scripts: an error that lists
the candidates as remedies). --occupied adopts every occupied path of the
named package.

Flags:
      --occupied   All occupied paths of the named package
  -n, --dry-run    Show the plan; change nothing
      --force      Overwrite differing package content without asking

Examples:
  dstow adopt ~/.zshrc zsh
  dstow adopt ~/.config/foo/foo.toml     # no package: pick from candidates
```

### repo (group)

```
Manage repos — where packages come from.

Usage:
  dstow repo <command>

Commands:
  add       Register a repo from a source (path, URL, github:owner/name)
  remove    Unregister a repo (deletes managed clones only)
  update    Download remote repo changes; touch nothing on disk
  upgrade   Fast-forward clean clones to what update downloaded
```

#### repo add

```
Register a repo — where packages come from.

Usage:
  dstow repo add <source> [flags]

Sources: a local directory path, a full URL/ssh form, or a qualified source
like github:owner/name (bare owner/name asks first; in scripts, qualify).
Remote sources clone into the managed directory; local paths are registered
in place and never modified.

Adding stows nothing: dstow announces the repo's packages and any shadowing
changes. Re-adding a present repo is a safe, announced no-op.

Flags:
      --stow   After adding, stow this repo's packages (exclusions apply)

Examples:
  dstow repo add ~/dotfiles
  dstow repo add github:rocne/dotfiles
  dstow repo add rocne/dotfiles --stow
```

#### repo remove

```
Unregister a repo. Managed clones are deleted; local-path repos are only
forgotten — your directory is never touched.

Usage:
  dstow repo remove <repo> [flags]

Refuses while the repo still has stowed links (offers unstow-then-remove),
and refuses to delete a managed clone holding work not present at its
source. Every refusal names its remedy.

Flags:
      --unstow   Unstow the repo's packages first, without prompting
      --force    Override both guards (unsaved work will be lost)
```

#### repo update / repo upgrade

```
Sync remote repos in two explicit phases. Neither ever runs on its own.

Usage:
  dstow repo update [<repo>...]     Download changes; alter nothing on disk
  dstow repo upgrade [<repo>...]    Fast-forward clean clones; report old -> new

With no repos named, both act on every remote repo. update touches the
network and nothing else — afterwards, status reports behind/ahead.
upgrade is fast-forward only: divergence or local work refuses loudly,
with no stash, merge, or rebase negotiation. upgrade never re-stows;
structural drift shows up in status. Linked files change the moment
upgrade moves them — update first, review, then upgrade.

Examples:
  dstow repo update
  dstow status
  dstow repo upgrade rocne/dotfiles
```

### list (leaf)

```
What is configured — repos, packages, targets, exclusions, sources.
Reads only configuration: instant, side-effect free, never inspects disk.
Deployment truth lives in 'dstow status'.

Usage:
  dstow list [flags]

Flags:
      --repos      Repos only (source, scheme, bulk-exclusion)
      --packages   Packages only (repo-attributed; shadowed duplicates marked)
      --json       Machine-readable listing
```

### info (leaf)

```
Everything dstow knows about one repo or package — from configuration and
metadata, never by inspecting targets (that is status's job).

Usage:
  dstow info <name> [flags]

Reports identity (repo, source, scheme, managed path, qualified name) and
configuration (effective target, dot-translation, ignores, declared
dependencies). Dependencies are declared by editing the metadata file;
info is how they are read.

Flags:
  -f, --field <field>   Only the named field(s); repeatable (alias: deps
                        for dependencies)
      --json            Machine-readable

Examples:
  dstow info zsh
  dstow info zsh -f dependencies
  dstow info dotfiles -f source -f scheme
```

### status (leaf)

```
What is deployed — expected links against what targets actually hold.

Usage:
  dstow status [<name>...] [flags]
  dstow status <path>

Names scope to packages or whole repos. Package states: stowed, partially
stowed, not stowed, occupied, damaged — plus a drifted marker when the
deployed shape differs from what current config would produce. damaged is
only ever claimed with ledger evidence. Remote repos also show
behind/ahead as of the last update. Unmet dependencies appear in package
detail.

For a path: what occupies it, who owns it (per the ledger), and — if
occupied — the packages that could adopt it, ranked.

Flags:
      --json   Machine-readable status

Examples:
  dstow status
  dstow status zsh
  dstow status ~/.zshrc      # the per-path view, adoption candidates incl.
```

### check / clean / rebuild (leaves)

```
Verify every ledgered link — instant, no tree walk.

Usage:
  dstow check [flags]

Classifies stale links: broken (destination gone) and orphaned (resolves
into a known repo, but no current package config would produce it). clean
executes exactly this report — the two can never disagree.

Flags:
      --json   Machine-readable report
```

```
Execute exactly what check reported.

Usage:
  dstow clean [flags]

Broken links are removed freely. Orphans are shown and confirmed (--yes
removes them without asking). Nothing else is ever touched.

Flags:
      --force   Remove orphans without confirmation in any context
```

```
Reconstruct a lost ledger by scanning configured targets for links into
known repos. The only full tree walk dstow has; explicit and rare.

Usage:
  dstow rebuild
```

### snippet (group)

```
Print canned POSIX-sh snippets to stdout. dstow never edits rc files.

Usage:
  dstow snippet <command>

Commands:
  rc            The shell-rc bootstrap: installs dstow iff absent — silent
                and network-free whenever dstow is already present
  hook <name>   A bundled helper hook (e.g. a loop-and-install script over
                declared dependencies) to drop into a hooks directory

Examples:
  dstow snippet rc >> ~/.bashrc
  dstow snippet hook install-deps
```

---

## Decision record (session of 2026-07-14)

Structural:

- **Shape rule** — groups vs leaves, as above. Guarantees no
  operand/subcommand collisions; retroactively justifies `repo update`
  nesting and killed `adopt candidates`.
- **Unified naming grammar** — packages/repos/sources are one grammar;
  suffix-unique resolution; repo-vs-package distinguishable by syntax;
  full grammar (incl. separator) delegated to the naming-grammar ticket.
- **Three read surfaces** — `list` enumerates config; `info` details one
  named thing (config+metadata, no disk); `status` is the only
  target-inspecting read.
- **`info` introduced** — `info <name> [-f <field>]`, brew/apt lineage; the
  §11-sanctioned doorway ("dependency query as a named-property read").
  Fields classify as identity (permanently read-only) vs configuration;
  classification is model-level, not command-level. Details → info ticket.
- **`dependencies` group dropped** — no add/remove/list commands. Declare
  by editing the metadata file (like every knob); read via
  `info -f dependencies`; live unmet-state in `status`. Write commands
  deferred to the v2 property store, where they can arrive as a designed
  family. dstow has zero config-mutation commands.

Ledger:

- **D1** — `repo update` / `repo upgrade` (apt danger-gradient; nesting
  frees top-level names and cannot be misread as self-update).
- **D2** — bare deploy verb = everything **with §1.2 confirmation**
  interactively (count + truncated list); hard error naming `--all`
  non-interactively; `--all` = explicit bulk, no prompt; `--yes`
  deliberately does not answer the bulk prompt. Bulk scopes: `--all`
  (global) or naming a repo (that repo).
- **D3** — dissolved: qualification is the name (suffix grammar), no
  `--repo` flag.
- **D4** — one `list`; no `repo list`.
- **D5** — `candidates` is not a command: per-path `status` shows ranked
  candidates; package-less `adopt` runs the interactive §1.2 choice
  (non-interactive: error listing candidates as remedies). Standing
  principle: a flag never changes a command's concept.
- **D6** — `snippet` is a group from day one (expandability; bare shows
  help): `rc` (§10.1), `hook <name>` (bundled helpers — new scope, noted
  to Bootstrap design).
- **D7** — long-canonical-plus-alias as a naming principle (glossary terms
  like `repo` stay canonical). Applied to info's field names
  (`dependencies`, alias `deps`).
- **D8** — `-n/--dry-run` on stow/unstow/restow/adopt.
- **D9** — `--yes` pre-answers confirmations of stated intent only; never
  resolves ambiguity, never bypasses guards. `--force` per-command, only
  where REQUIREMENTS names an override.
- **D10** — `clean` takes no scope flags; executes exactly check's plan.
- **D11** — add-and-stow is `repo add --stow` (flag extends, doesn't
  change concept).
- **D12** — no CLI flag twins for config knobs; refusals name the knob.
- **D13** — `--json` per-command (list, info, status, check), exact shape
  reserved for Output design.
- **D14** — absorbed into per-path `status`.

## Handoffs this resolution creates

1. **Naming-grammar ticket (rescope)**: sources → the full naming grammar —
   qualified package names, suffix-unique resolution, the repo/package
   separator, names-vs-paths rule. Must also resolve the **§2.4 tension**:
   REQUIREMENTS says bare names resolve first-on-path (shadowing);
   the session leaned toward ambiguity-errors instead — that is a
   requirements amendment if taken, decide deliberately.
2. **Info ticket (rescope)**: dependency-query ticket becomes "info: the
   property read" — field vocabulary, identity/configuration
   classification + reserved names, §9.2.4 machine contract (exit codes,
   loop-and-install), `--json` vs `-f` interplay.
3. **Output design**: confirmation-prompt presentation (truncated package
   lists), refusal wording for D12 knob-refusals, `--json` shapes.
4. **Bootstrap design**: `snippet rc` naming + contents; the bundled
   `snippet hook <name>` library (which hooks ship in v1).
5. **CONTEXT.md** (at info-ticket resolution): mint *field*, *identity
   field*, *configuration field*.

## Requirements coverage

| REQUIREMENTS | Surface element |
|---|---|
| §3 deploy verbs, independence, exit | `stow`/`unstow`/`restow`, `--all`, nonzero-on-any-failure |
| §5.1–5.2 add, sources, no-stow default, opt-in | `repo add [--stow]` |
| §5.3 remove + guards | `repo remove [--unstow] [--force]` |
| §6 two-phase sync | `repo update` / `repo upgrade` |
| §7.1 config view | `list [--repos|--packages]` |
| §7.2 states, drift, sync state, per-path detail | `status [<name>...|<path>]` |
| §8.2–8.4 check/clean/rebuild | `check`, `clean`, `rebuild` |
| §8.5 adopt + scopes + candidates + confirm/force | `adopt`, `--occupied`, package-less candidate flow, per-path `status` |
| §9.2.4 dependency query | `info -f dependencies` (contract → info ticket) |
| §10.1 snippet | `snippet rc` |
| §1.2 confirm-unless-unambiguous | bulk confirms, candidate choice, source confirms; `--all` and qualification as the unambiguous forms |
| §2.3–2.4 DSTOW_PATH, shadowing | env docs; suffix qualification (grammar ticket) |
| §9.1 hooks | no commands — metadata-dir files firing around verbs |

Not surfaced (deliberately): config-knob flags (D12); first-run
announcement (§3.6 — output, not surface); folding (global config only).
