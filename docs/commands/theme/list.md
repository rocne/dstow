# dstow theme list

<!-- dstow:short -->
List the available themes: bundled presets and your themes dir, active theme marked
<!-- /dstow:short -->

<!-- dstow:long -->
List every theme the loader resolves: the bundled presets and the theme files
in your themes directory, with the active theme marked.

These are the names 'dstow theme emit' accepts.
<!-- /dstow:long -->
