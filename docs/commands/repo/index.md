# dstow repo

<!-- dstow:short -->
Manage repos: add, remove, update, upgrade
<!-- /dstow:short -->

<!-- dstow:long -->
Manage repos — where packages come from.

Environment:
  DSTOW_PATH   Colon-separated directories registered as session repos —
               additions to the repo set for this shell only; no priority
<!-- /dstow:long -->

## Topics

- `add` — register a repo from a source
- `remove` — unregister a repo
- `update` — download remote changes
- `upgrade` — fast-forward clean clones
