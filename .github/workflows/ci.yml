name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  # Secret scan (rocne/release-ci#46, audit row 19 — should, mandatory-defended).
  # Full history, every ref; bare pinned+checksummed binary (not gitleaks-action,
  # which needs a paid license on org accounts). Version bumps are a manual edit
  # of the URL + checksum together — Dependabot doesn't track release downloads.
  gitleaks:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0 # full history — the scan walks every commit
          persist-credentials: false
      - name: Download gitleaks (pinned, checksum-verified)
        run: |
          curl -fsSL --proto '=https' --tlsv1.2 -o gitleaks.tar.gz \
            https://github.com/gitleaks/gitleaks/releases/download/v8.30.1/gitleaks_8.30.1_linux_x64.tar.gz
          echo "551f6fc83ea457d62a0d98237cbad105af8d557003051f41f3e7ca7b3f2470eb  gitleaks.tar.gz" | sha256sum -c -
          tar -xzf gitleaks.tar.gz gitleaks
      - name: Scan full history for secrets
        run: ./gitleaks git . --redact --no-banner --log-opts="--all"

  # The hermetic suite, on every platform we ship a binary for.
  #
  # GoReleaser builds linux+darwin x amd64+arm64. macos-latest is arm64 and
  # ubuntu-24.04-arm is linux/arm64, so both axes are exercised without a
  # fourth runner. fail-fast is off: a darwin failure must not hide a linux
  # one.
  test:
    name: test (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, ubuntu-24.04-arm, macos-latest]
    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-go@v7
        with:
          go-version-file: go.mod

      - name: Build
        run: go build ./...

      - name: Vet
        run: go vet ./...

      - name: Test
        run: go test -count=1 ./...

  e2e:
    needs: test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-go@v7
        with:
          go-version-file: go.mod

      - name: Pre-release e2e
        run: ./test/run-e2e.sh
